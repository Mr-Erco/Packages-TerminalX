
  # Create Packages Page

  This is a code bundle for Create Packages Page. The original project is available at https://www.figma.com/design/2N5ze3qVxDa8vNojtDfZUQ/Create-Packages-Page.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  